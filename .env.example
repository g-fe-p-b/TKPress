# Example environment variables for TKPress
# Copy to `.env` and adjust values before running

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=your_password
DB_NAME=tkpress

# App
PORT=3000
SESSION_SECRET=change_this_secret
NODE_ENV=development
